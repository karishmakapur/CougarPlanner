<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfirstapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cougarplanner"
        android:label="Cougar Planner"
        android:roundIcon="@drawable/cougarplanner"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".DailyActivity"></activity>
        <activity android:name=".SemesterlyActivity" />
        <activity android:name=".MonthlyActivity" />
        <activity android:name=".WeeklyActivity" />
        <activity android:name=".FillProfileActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".AddCourseActivity" />
        <activity android:name=".AddFriendActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>